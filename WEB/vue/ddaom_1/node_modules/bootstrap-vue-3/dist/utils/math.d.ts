/**
 * A simple wrapper for Math.min
 */
export declare const mathMin: (...values: number[]) => number;
/**
 * A simple wrapper for Math.max
 */
export declare const mathMax: (...values: number[]) => number;
